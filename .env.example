# --- Database (MySQL) ---
DATABASE_URL="mysql://user:password@localhost:3306/portfolio_rave"

# --- Nodemailer (optional contact notification email) ---
# Use an app password for Gmail SMTP, or any SMTP host.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="465"
SMTP_USER="you@example.com"
SMTP_PASS="your-app-password"
NOTIFY_TO="you@example.com"

# --- Public site URL (used in OG meta, sitemap) ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
